const item = {
  id: 0,
  title: 'Hello World',
  completed: false,
};

// const id = item.id;
// const title = item.title;
// const completed = item.completed;
const { id, title, completed } = item;

console.log(id, title, completed); // 0 Hello World false
