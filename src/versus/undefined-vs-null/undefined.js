// Undefined: The variable is not defined or not exists

let name;

console.log(name); // undefined

const obj = {
  name: 'John',
};

console.log(obj.age); // undefined

console.log(typeof undefined); // undefined
